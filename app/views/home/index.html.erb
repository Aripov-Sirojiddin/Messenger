<% provide(:title, "Messenger: Home") %>

<div class="d-flex flex-column align-items-center">
  <% if logged_in? %>
    <p>User feed will go here</p>
  <% else %>
    <%= render "sessions/login_form" %>
  <% end %>

</div>